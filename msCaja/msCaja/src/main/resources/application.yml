spring:
  application:
    name: msCaja
  datasource:
    url: jdbc:mysql://localhost:3306/caja_db
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

server:
  port: 8091

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true