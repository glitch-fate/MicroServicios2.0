spring:
  application:
    name: msSeguros
  datasource:
    url: jdbc:mysql://localhost:3306/seguros_db
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8089

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true