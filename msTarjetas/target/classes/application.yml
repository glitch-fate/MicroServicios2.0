spring:
  application:
    name: msTarjetas
  datasource:
    url: jdbc:mysql://localhost:3306/tarjetas_db
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

server:
  port: 8088

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true